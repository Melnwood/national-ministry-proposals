{
  "name": "national-ministries-app",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "JV National Ministries — grant lifecycle app (v2, role-based single app)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "vite": "^5.4.10"
  }
}
